process TEST = {

         source = EmptySource {
           untracked int32 maxEvents = 1
           untracked uint32 firstRun=60123
           untracked uint32 numberEventsInRun=1
         }

         service = PoolDBOutputService{
            string connect = "sqlite_file:mydata.db"    
            string timetype = "runnumber"    
            untracked uint32 authenticationMethod = 1
            untracked string catalog = "file:mycatalog.xml"
            VPSet toPut={
             {untracked string containerName = "EcalGainRatios"
               string tag ="preliminary"}
            }
         }

         module ecalModule = createEcalObject{
            string calibType = "EcalGainRatios"
            string inpFileName = "/afs/cern.ch/cms/ECAL/testbeam/pedestal/2006/GAINRATIO/laboratory_gainratio_sm22.txt"
         }

         path p = { ecalModule }

         module print = AsciiOutputModule{ }
         endpath ep = { print }

       }
